// Ponto de entrada do pacote de config compartilhada (env schema, constantes).
// O schema de validação de ambiente (Zod) é adicionado no passo 5 (Auth) deste roadmap.
export const NOTAA_CONFIG_VERSION = '0.1.0';
