// STUB — consumidores pg-boss (correção de redação, lotes do Profiler), conforme
// docs/03-arquitetura.md §3.3. Reusa @notaa/engine-* e o módulo `ai` da API
// (mesmo portão de IA — Interceptor compartilhado, doc 03 §4).
export const WORKER_PLACEHOLDER = true;
